<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <p><a href="/logout">Log Out</a></p>

      <h1>Welcome <%= Helper.current_user(session).username %></h1>
      <p><%= Helper.current_user(session).balance %></p>

  </body>
</html>
